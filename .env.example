# Placeholders only; use workload identity and secret management.
AWS_REGION=us-east-1
AWS_ROLE_ARN=arn:aws:iam::<account-id>:role/<sagemaker-execution-role>
TRAIN_S3_URI=s3://<bucket>/demand-training/
SM_OUTPUT_S3_URI=s3://<bucket>/demand-models/
MODEL_PATH=/models/model.joblib
